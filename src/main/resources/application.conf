test_db = {
  connectionPool = "HikariCP"
  dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
  properties = {
    serverName = "localhost"
    portNumber = "5432"
    databaseName = "slick"
    user = "slick"
    password = "testes"
  }
  numThreads = 10
}

# db_url = "jdbc:postgresql://db:5432/trollabot?user=postgres&password=password"
db_url = ${DATABASE_URL}

debug = true

twitch {
  irc {
   server = "irc.chat.twitch.tv"
    port = 6697
    token = ${TROLLABOT_TOKEN}
    username = "trollabot"
  }
}

// Include passwords and tokens
include "secret.conf"